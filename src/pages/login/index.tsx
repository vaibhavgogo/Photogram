import * as React from 'react';

interface ILoginProps {
  // define your props here
}

const Post: React.FunctionComponent<ILoginProps> = (props) => {
  return (
    <div>
    Login
    </div>
  );
};

export default Login